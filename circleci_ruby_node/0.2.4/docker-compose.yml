version: '3'
services:
  circleci_ruby_node_test: &base
    image: $IMAGE_NAME
    build:
      dockerfile: Dockerfile.test
      context: ./
      args:
        - VERSION=$VERSION
        - IMAGE=$IMAGE
        - DOCKER_ID_USER=$DOCKER_ID_USER
    working_dir: /home/app/app
    command: /home/test/test.sh
