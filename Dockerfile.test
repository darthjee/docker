ARG DOCKER_ID_USER
ARG IMAGE
ARG VERSION

FROM $DOCKER_ID_USER/$IMAGE:$VERSION

ARG USER_NAME
ARG IMAGE
ARG VERSION

COPY $IMAGE/$VERSION/home ./
COPY $IMAGE/$VERSION/test /home/test

USER root
RUN chown $USER_NAME:$USER_NAME /home/* -R
USER $USER_NAME

